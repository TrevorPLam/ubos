# Auth API

## Implemented
- GET /api/login
- GET /api/logout
- GET /api/auth/user

## Notes
- Auth uses `x-user-id` / `x-user` header or `ubos_user_id` HttpOnly cookie.

## Gaps vs plan
- Real identity, RBAC, sessions, OIDC.
